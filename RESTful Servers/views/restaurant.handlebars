
<body>
    <header>
      <nav>
        <h1>RestoDirect</h1>
       <a href="#">CONTACT</a>
      </nav>
    </header>

        {{#each restaurants}}
    <section>
        <a href="/restaurants"><i class="fas fa-2x fa-arrow-left"></i></a>
        <h1 class="restName">{{this.name}}<i class="fas fa-pencil-alt"></i></h1>
    </section>
    
    <section class="editForm">
        <section class="forms hidden">
        <i class="fas fa-2x fa-times"></i>
        <i class="fas fa-2x fa-arrow-left" id="exitForm"></i>
        <form action="" method="post" class="restaurantDetails hidden">
            <div>
                <label for="name">Restaurant Name</label>
                <input type="text" name="name" id="name" value="{{this.name}}">
            </div>
            <div>
                <label for="imageURL">Image Link (URL)</label>
                <input type="url" name="imageURL" id="imageURL" value="{{this.image}}">
            </div>
         <input type="submit" value="Submit Details">
          </form>

        <form action="" method="post" class=" restaurantMenus hidden">
            {{#each menus}}
            <div>
                <label for="menuName">Menu Name</label>
                <input type="text" name="menuName" id="menuName" value="{{this.title}}">
            </div>
            {{/each}}
         <input type="submit" value="Submit Details">
         
        </form> 

        {{!-- <form action="" method="post" class=" restaurantMenuItems hidden">
            {{#each menus}}
                <p>{{this.title}}</p>
            <div>
            {{#each menuItems}}
                <label for="menuItem">Menu Item + Price</label>
                <input type="text" name="menuName" id="menuName" value="{{this.name}}">
                <input type="text" name="menuName" id="menuName" value="{{this.price}}">
            </div>
            {{/each}}
            {{/each}}
         <input type="submit" value="Submit Details">
         
        </form>  --}}
        </section>

        <div class="editBtns">
        <button id="editDetails">Edit Restaurant Details</button>
        <button id="editMenus">Edit Restaurant Menus</button>
        <button id="editMenuItems">Edit Restaurant Items</button>
        </div>
       

        
    </section>

    <div class="container">
        <figure>
            <img src="{{this.image}}" width="570" height="500" alt="">
            <figcaption>
            {{#each menus}}
                <p class="menu">{{this.title}}</p>
                <ul class="menuItems" id="{{this.id}}">
                    {{#each menuItems}}
                    <li>{{this.name}} £{{this.price}}</li>
               {{/each}}
                </ul>
                <br>
            {{/each}}
            </figcaption>
        </figure>
    </div>
    <script src="js/restaurant.js"></script> 
    {{/each}}



</body>

