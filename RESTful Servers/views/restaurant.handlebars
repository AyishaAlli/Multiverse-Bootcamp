
<body>
    <header>
      <nav>
        <h1>RestoDirect</h1>
       <a href="#">CONTACT</a>
      </nav>
    </header>

        {{#each restaurants}}
    <section>
        <a href="/restaurants"><i class="fas fa-2x fa-arrow-left"></i></a>
        <h1 class="restName">{{this.name}}<i class="fas fa-pencil-alt"></i></h1>
    </section>
    
    <section class="editForm">
        <i class="fas fa-2x fa-times"></i>
        <form action="" method="post">
            <div>
                <label for="name">Restaurant Name</label>
                <input type="text" name="name" id="name" value="{{this.name}}">
            </div>
            <div>
                <label for="imageURL">Image Link (URL)</label>
                <input type="url" name="imageURL" id="imageURL" value="{{this.image}}">
            </div>
            {{#each menus}}
            <div>
                <label for="menuName">Menu Name</label>
                <input type="text" name="menuName" id="menuName" value="{{this.title}}">
            </div>
            {{#each menuItems}}
            <div> 
                <label for="menuItem">Item + Price</label>
                <input type="text" name="menuItem" id="menuItem" value="{{this.name}}">
                <input type="text" name="menuItem" id="menuItem" value="{{this.price}}">
            </div>
             {{/each}}
            {{/each}}
         <input type="submit" value="Submit Details">
        </form> 
        
    </section>

    <div class="container">
        <figure>
            <img src="{{this.image}}" width="570" height="500" alt="">
            <figcaption>
            {{#each menus}}
                <p class="menu">{{this.title}}</p>
                <ul class="menuItems hidden" id="{{this.id}}">
                    {{#each menuItems}}
                    <li>{{this.name}} £{{this.price}}</li>
               {{/each}}
                </ul>
                <br>
            {{/each}}
            </figcaption>
        </figure>
    </div>
    <script src="js/restaurant.js"></script> 
    {{/each}}



</body>

